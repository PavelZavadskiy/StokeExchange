{
  "name": "StokeExchange",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "license": "ISC",
  "scripts": {
    "test": "truffle test",
    "compile": "truffle compile --all",
    "migrate-rinkeby": "truffle migrate --network rinkeby --reset",
    "verify-contract": "truffle run verify StokeExchange --network rinkeby",
    "verify-first": "truffle run verify FirstToken --network rinkeby",
    "verify-second": "truffle run verify SecondToken --network rinkeby",
    "verify-third": "truffle run verify ThirdToken --network rinkeby"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.3",
    "@openzeppelin/test-helpers": "^0.5.15",
    "@truffle/hdwallet-provider": "^1.7.0",
    "bn.js": "^5.2.0",
    "dotenv": "^10.0.0",
    "ethers": "^4.0.49",
    "truffle-assertions": "^0.9.2"
  },
  "devDependencies": {
    "truffle-plugin-verify": "^0.5.18"
  }
}
